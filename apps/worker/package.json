{
  "name": "@plan-self/worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/worker.ts",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node --test"
  },
  "dependencies": {
    "@plan-self/config": "^0.1.0",
    "bullmq": "^5.16.0",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "ts-node": "^10.9.2",
    "tsx": "^4.22.3"
  }
}
